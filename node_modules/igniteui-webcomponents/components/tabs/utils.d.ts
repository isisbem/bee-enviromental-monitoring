export declare const observerConfig: MutationObserverInit;
/**
 * Returns all targets with attribute mutations matching `tags`
 */
export declare function getAttributesForTags<T extends Element>(records: MutationRecord[], ...tags: string[]): T[];
/**
 * Returns all targets with childList mutations matching tags.
 * If `root` is specified, returns only targets that are direct children
 * of `root`.
 */
export declare function getNodesForTags<T extends Element>(records: MutationRecord[], root?: Element, ...tags: string[]): {
    addedNodes: T[];
    removedNodes: T[];
} | null;
