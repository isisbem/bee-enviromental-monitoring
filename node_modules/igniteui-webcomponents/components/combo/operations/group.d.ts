import { DataController } from '../controllers/data.js';
import { GroupingDirection, Keys, Values } from '../types.js';
export default class GroupDataOperation<T extends object> {
    protected orderBy: Map<string, number>;
    protected resolveValue(record: T, key: Keys<T>): T[keyof T];
    protected compareValues(first: Values<T>, second: Values<T>): number;
    protected compareObjects(first: T, second: T, key: Keys<T>, direction: GroupingDirection): number;
    apply(data: T[], controller: DataController<T>): any[];
}
